<h1>Employers#index</h1>

<table>
  <tr>
    <th>Номер</th>
    <th>Имя</th>
    <th>Фамилия</th>
  </tr>

  <% @employers.each do |employee| %>
    <tr>
      <td><%= employee.id %></td>
      <td><%= employee.name %></td>
      <td><%= employee.last_name %></td>
    </tr>
  <% end %>

</table>
